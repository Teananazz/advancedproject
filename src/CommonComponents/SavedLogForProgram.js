import * as ReactDOMServer from 'react-dom/server';

const SavedLogForProgram =
(props) => {
    var ReactDOMServer = require('react-dom/server');


  
    var check = ReactDOMServer.renderToString(props);
    console.log(check);
   



}





export default SavedLogForProgram;